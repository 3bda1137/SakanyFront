import { RentSaleDirectiveDirective } from './rent-sale-directive.directive';

describe('RentSaleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RentSaleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
