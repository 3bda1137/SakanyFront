import { PriceColorDirectiveDirective } from './price-color-directive.directive';

describe('PriceColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
